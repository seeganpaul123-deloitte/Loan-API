loan-status.query = insert into "LOAN_APPLICATION"."PUBLIC"."LOAN_STATUS"(LOAN_ID, USERID, STATUS, REASON, AMOUNT) values (:user_id, :loan_id, :status, :reason, :amount)
select-loan-status.query = select * from "LOAN_APPLICATION"."PUBLIC"."LOAN_STATUS" where USERID = :UserId